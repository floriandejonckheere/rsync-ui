<% content_for :title do %>
  <%= I18n.t("jobs.index.title") %>
<% end %>

<% content_for :subtitle do %>
  <%= I18n.t("jobs.index.subtitle") %>
<% end %>

<% content_for :actions do %>
  <%= link_to new_job_path, title: I18n.t("jobs.index.new"), class: "btn-icon-outline btn-icon-lg" do %>
    <%= lucide_icon "plus", class: "h-6 w-6" %>
  <% end %>
<% end %>

<% if @jobs.empty? %>
  <div class="card flex flex-col items-center justify-center py-16 gap-4 text-gray-400">
    <%= lucide_icon "arrow-right-left", class: "h-12 w-12" %>

    <p class="text-lg">
      <%= I18n.t("jobs.index.empty") %>
    </p>
  </div>
<% else %>
  <div class="card p-0 overflow-hidden">
    <table class="w-full text-sm">
      <thead>
        <tr class="border-b border-gray-100 dark:border-gray-700">
          <th class="px-6 py-4 text-left font-medium text-gray-500 dark:text-gray-400">
            <%= I18n.t("jobs.table.name") %>
          </th>

          <th class="px-6 py-4 text-left font-medium text-gray-500 dark:text-gray-400">
            <%= I18n.t("jobs.table.source") %>
          </th>

          <th class="px-6 py-4 text-left font-medium text-gray-500 dark:text-gray-400">
            <%= I18n.t("jobs.table.destination") %>
          </th>

          <th class="px-6 py-4 text-left font-medium text-gray-500 dark:text-gray-400">
            <%= I18n.t("jobs.table.schedule") %>
          </th>

          <th class="px-6 py-4 text-left font-medium text-gray-500 dark:text-gray-400">
            <%= I18n.t("jobs.table.enabled") %>
          </th>

          <th class="px-6 py-4"></th>
        </tr>
      </thead>

      <tbody>
        <% @jobs.each do |job| %>
          <%= render "job", job: %>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
