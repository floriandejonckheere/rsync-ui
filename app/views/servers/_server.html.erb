<tr
  class="
    border-b border-gray-100 dark:border-gray-700 last:border-b-0
    hover:bg-gray-50 dark:hover:bg-gray-700/50
  "
>
  <td class="px-6 py-4">
    <div class="font-medium text-gray-900 dark:text-gray-100">
      <%= server.name %>
    </div>

    <% if server.description.present? %>
      <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">
        <%= server.description %>
      </div>
    <% end %>
  </td>

  <td class="px-6 py-4 font-mono text-gray-600 dark:text-gray-300">
    <%= server.host %>:<%= server.port %>
  </td>

  <td class="px-6 py-4 font-mono text-gray-600 dark:text-gray-300">
    <%= server.username %>
  </td>

  <td class="px-6 py-4">
    <div class="flex gap-2">
      <% if server.password.present? %>
        <span
          class="
            inline-flex items-center gap-1 rounded-full bg-blue-50
            dark:bg-blue-900/30 px-2 py-1 text-xs text-blue-700
            dark:text-blue-300
          "
        >
          <%= lucide_icon "key-round", class: "h-3 w-3" %>
          <%= I18n.t("servers.table.password") %>
        </span>
      <% end %>

      <% if server.ssh_key.present? %>
        <span
          class="
            inline-flex items-center gap-1 rounded-full bg-green-50
            dark:bg-green-900/30 px-2 py-1 text-xs text-green-700
            dark:text-green-300
          "
        >
          <%= lucide_icon "key", class: "h-3 w-3" %>
          <%= I18n.t("servers.table.ssh_key") %>
        </span>
      <% end %>
    </div>
  </td>

  <td class="px-6 py-4">
    <div class="flex items-center justify-end gap-2">
      <%= link_to edit_server_path(server),
                  class: "btn-icon-outline btn-icon-md",
                  title: I18n.t("servers.actions.edit") do %>
        <%= lucide_icon "pencil", class: "h-4 w-4" %>
      <% end %>

      <%= link_to server_path(server),
                  class: "btn-icon-outline btn-icon-md text-red-600 hover:text-red-700 dark:text-red-400",
                  title: I18n.t("servers.actions.delete"),
                  data: {
                    "turbo-method": :delete,
                    "turbo-confirm": I18n.t("servers.actions.delete_confirm"),
                    "turbo-confirm-title": I18n.t("servers.actions.delete_confirm_title"),
                    "turbo-confirm-destructive": true,
                  } do %>
        <%= lucide_icon "trash-2", class: "h-4 w-4" %>
      <% end %>
    </div>
  </td>
</tr>
