<div id="server_fingerprint" class="grow">
  <% if fingerprint.present? %>
    <p class="font-mono text-sm truncate"><%= fingerprint %></p>
  <% else %>
    <p class="text-sm text-muted-foreground">
      <%= I18n.t("servers.form.fingerprint_none") %>
    </p>
  <% end %>
</div>
