---
en:
  dashboard:
    jobs:
      ago: ago
      last_job: Last job
      next_job: Next job
      no_jobs: No jobs scheduled
      no_runs: No runs yet
      runs_in: in %{time}
    repositories:
      local: Local
      remote: Remote
      title: Repositories
      total:
        one: 1 repository
        other: "%{count} repositories"
        zero: No repositories
    repositories_and_jobs:
      title: Repositories and jobs
    running_jobs:
      no_running: No jobs running
      title: Running jobs
    server_health:
      cores: "%{count} cores"
      cpu: CPU
      disabled: Resource usage disabled
      disk: Disk
      failed: 'Failed to retrieve resource usage: %{error}'
      last_probed: "%{time} ago"
      load: Load
      memory: Memory
      never_probed: Never probed
      no_servers: No servers configured
      of: of
      path: 'Path: %{path}'
      title: Server health
      uptime: Up %{time}
    status:
      degraded: Degraded
      healthy: Healthy
      period: Past 24 hours
      title: Overall status
      total_runs:
        one: 1 run in the past 24 hours
        other: "%{count} runs in the past 24 hours"
        zero: No runs in the past 24 hours
      unknown: Unknown
    storage:
      title: Storage
    subtitle: Monitor recent activity
    title: Dashboard
