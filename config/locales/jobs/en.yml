---
en:
  activerecord:
    errors:
      models:
        job:
          attributes:
            base:
              not_both_remote: Source and destination cannot both be remote
            destination_repository:
              read_only: cannot be read-only
              same_as_source: must be different from the source repository
            schedule:
              invalid: must be a valid cron expression
  jobs:
    actions:
      delete: Delete job
      delete_confirm: Are you sure you want to delete this job? This action cannot be undone.
      delete_confirm_title: Delete job
      disable: Disable job
      edit: Edit job
      enable: Enable job
      run: Run job
    create:
      success: Job was successfully created.
    destroy:
      success: Job was successfully deleted.
    edit:
      title: Edit job
    enabled:
      'false': Disabled
      'true': Enabled
    form:
      add_pattern: Add pattern
      advanced_options: Advanced options
      basic_options: Basic options
      custom_options: Custom options
      description: Description
      description_placeholder: Optional description for this job
      destination_repository: Destination repository
      destination_repository_placeholder: Select a destination repository
      destination_repository_search_placeholder: Search repositories...
      details: Details
      enabled: Enabled
      exclude_patterns: Exclude patterns
      hooks: Hooks
      hooks_arguments: Arguments
      hooks_arguments_help: |
        The following variables will be substituted in the hook arguments:
        - {job_id}
        - {job_name}
        - {trigger}
        - {job_sequence}
        - {source_id}
        - {source_name}
        - {destination_id}
        - {destination_name}
        - {started_at}
        - {user_id}
        - {user_name}

        Post/success/failure hooks also have:
        - {completed_at}
        - {duration}
        - {status}
        - {error}
      hooks_command: Command
      hooks_enabled: Enabled
      hooks_failure: Failure hook
      hooks_post: Post-hook
      hooks_pre: Pre-hook
      hooks_success: Success hook
      include_exclude_patterns: Include/exclude patterns
      include_patterns: Include patterns
      name: Name
      name_placeholder: Daily backup
      notifications: Notifications
      notifications_empty_html: No notifications configured. %{link} to add one.
      notifications_empty_link: Create one
      notifications_on_failure: On failure
      notifications_on_start: On start
      notifications_on_success: On success
      opt_acls:
        description: Preserve ACLs
        flag: "--acls"
      opt_append:
        description: Append data onto shorter files
        flag: "--append"
      opt_archive:
        description: Archive mode
        flag: "--archive"
      opt_arguments:
        description: Additional arguments
        placeholder: e.g. --bwlimit=1000
      opt_backup:
        description: Make backups
        flag: "--backup"
      opt_checksum:
        description: Skip based on checksum
        flag: "--checksum"
      opt_compress:
        description: Enable compression
        flag: "--compress"
      opt_delete:
        description: Delete extra files on destination
        flag: "--delete"
      opt_delete_excluded:
        description: Delete excluded files on destination
        flag: "--delete-excluded"
      opt_devices:
        description: Preserve device numbers
        flag: "--devices"
      opt_dry_run:
        description: Dry run
        flag: "--dry-run"
      opt_existing:
        description: Only update existing files on destination
        flag: "--existing"
      opt_group:
        description: Preserve group ownership
        flag: "--group"
      opt_hard_links:
        description: Preserve hard links
        flag: "--hard-links"
      opt_ignore_existing:
        description: Ignore existing files on destination
        flag: "--ignore-existing"
      opt_inplace:
        description: Update files in-place
        flag: "--inplace"
      opt_itemize_changes:
        description: Show itemized changes list
        flag: "--itemize-changes"
      opt_links:
        description: Preserve symbolic links
        flag: "--links"
      opt_numeric_ids:
        description: Don't map uid/gid values
        flag: "--numeric-ids"
      opt_one_file_system:
        description: Do not leave filesystem
        flag: "--one-file-system"
      opt_owner:
        description: Preserve ownership
        flag: "--owner"
      opt_partial:
        description: Keep partially transferred files
        flag: "--partial"
      opt_perms:
        description: Preserve permissions
        flag: "--perms"
      opt_progress:
        description: Show progress
        flag: "--progress"
      opt_recursive:
        description: Recurse into directories
        flag: "--recursive"
      opt_relative:
        description: Relative path names
        flag: "--relative"
      opt_rsync_path:
        description: Alternate rsync path
        placeholder: e.g. /usr/local/bin/rsync
      opt_secluded_args:
        description: Protect remote args
        flag: "--secluded-args"
      opt_size_only:
        description: Size only
        flag: "--size-only"
      opt_specials:
        description: Preserve special files
        flag: "--specials"
      opt_superuser:
        description: Run as superuser
      opt_times:
        description: Preserve timestamps
        flag: "--times"
      opt_update:
        description: Skip newer files
        flag: "--update"
      opt_verbose:
        description: Verbose
        flag: "--verbose"
      opt_xattrs:
        description: Preserve extended attributes
        flag: "--xattrs"
      pattern_placeholder: e.g. *.log
      preview: Preview
      repositories: Repositories
      schedule: Schedule
      schedule_placeholder: 0 2 * * *
      schedule_preview_invalid: Invalid cron expression
      schedule_preview_placeholder: Enter a cron expression to see a preview
      source_repository: Source repository
      source_repository_placeholder: Select a source repository
      source_repository_search_placeholder: Search repositories...
      submit_create: Create job
      submit_update: Update job
    index:
      empty: No jobs have been configured yet.
      new: New job
      search:
        no_results:
          title: No jobs found
        placeholder: Search by name or description...
      subtitle: Manage synchronization tasks between repositories
      title: Jobs
    new:
      subtitle: Add a new synchronization job
      title: New job
    table:
      destination: Destination
      name: Name
      schedule: Schedule
      source: Source
    title: Jobs
    update:
      success: Job was successfully updated.
