---
en:
  activerecord:
    errors:
      models:
        job:
          attributes:
            destination_repository:
              read_only: cannot be read-only
              same_as_source: must be different from the source repository
            schedule:
              invalid: must be a valid cron expression
        repository:
          attributes:
            server:
              blank: must be provided for remote repositories
              present: must be absent for local repositories
        server:
          attributes:
            base:
              exclusive_credentials: Exactly one of password or SSH key must be provided.
            ssh_key:
              ssh_key_invalid: is not a valid OpenSSH private key.
              ssh_key_passphrase: must not be protected by a passphrase.
  application:
    name: Rsync UI
  auth:
    sign_out: Sign out
  configurations:
    index:
      cancel: Cancel
      default: Default
      depends_on: Depends on
      edit: Edit
      revert: Revert to default
      subtitle: Manage system-wide configuration settings
      title: Configuration
      update: Update
      value_placeholder: Enter configuration value
    title: Configuration
    update:
      error: Failed to update configuration
      success: Configuration updated successfully
  confirm_dialog:
    cancel: Cancel
    confirm: Confirm
    title: Confirm
  dashboard:
    subtitle: Monitor recent activity
    title: Dashboard
  flash:
    types:
      alert: Alert
      error: Error
      notice: Notice
      success: Success
      warning: Warning
  jobs:
    actions:
      delete: Delete job
      delete_confirm: Are you sure you want to delete this job? This action cannot be undone.
      delete_confirm_title: Delete job
      edit: Edit job
    create:
      success: Job was successfully created.
    destroy:
      success: Job was successfully deleted.
    edit:
      title: Edit job
    enabled:
      'false': Disabled
      'true': Enabled
    form:
      advanced_options: Advanced options
      basic_options: Basic options
      custom_options: Custom options
      description: Description
      description_placeholder: Optional description for this job
      destination_repository: Destination repository
      destination_repository_placeholder: Select a destination repository
      details: Details
      enabled: Enabled
      name: Name
      name_placeholder: Daily backup
      opt_acls:
        description: Preserve ACLs
        flag: "--acls"
      opt_append:
        description: Append data onto shorter files
        flag: "--append"
      opt_archive:
        description: Archive mode
        flag: "--archive"
      opt_arguments:
        description: Additional arguments
        placeholder: e.g. --bwlimit=1000
      opt_backup:
        description: Make backups
        flag: "--backup"
      opt_checksum:
        description: Skip based on checksum
        flag: "--checksum"
      opt_compress:
        description: Enable compression
        flag: "--compress"
      opt_delete:
        description: Delete extra files on destination
        flag: "--delete"
      opt_delete_excluded:
        description: Delete excluded files on destination
        flag: "--delete-excluded"
      opt_devices:
        description: Preserve device numbers
        flag: "--devices"
      opt_dry_run:
        description: Dry run
        flag: "--dry-run"
      opt_existing:
        description: Only update existing files on destination
        flag: "--existing"
      opt_group:
        description: Preserve group ownership
        flag: "--group"
      opt_hard_links:
        description: Preserve hard links
        flag: "--hard-links"
      opt_ignore_existing:
        description: Ignore existing files on destination
        flag: "--ignore-existing"
      opt_inplace:
        description: Update files in-place
        flag: "--inplace"
      opt_itemize_changes:
        description: Show itemized changes list
        flag: "--itemize-changes"
      opt_links:
        description: Preserve symbolic links
        flag: "--links"
      opt_numeric_ids:
        description: Don't map uid/gid values
        flag: "--numeric-ids"
      opt_one_file_system:
        description: Do not leave filesystem
        flag: "--one-file-system"
      opt_owner:
        description: Preserve ownership
        flag: "--owner"
      opt_partial:
        description: Keep partially transferred files
        flag: "--partial"
      opt_perms:
        description: Preserve permissions
        flag: "--perms"
      opt_progress:
        description: Show progress
        flag: "--progress"
      opt_recursive:
        description: Recurse into directories
        flag: "--recursive"
      opt_relative:
        description: Relative path names
        flag: "--relative"
      opt_rsync_path:
        description: Alternate rsync path
        placeholder: e.g. /usr/local/bin/rsync
      opt_secluded_args:
        description: Protect remote args
        flag: "--secluded-args"
      opt_size_only:
        description: Size only
        flag: "--size-only"
      opt_specials:
        description: Preserve special files
        flag: "--specials"
      opt_superuser:
        description: Run as superuser
      opt_times:
        description: Preserve timestamps
        flag: "--times"
      opt_update:
        description: Skip newer files
        flag: "--update"
      opt_verbose:
        description: Verbose
        flag: "--verbose"
      opt_xattrs:
        description: Preserve extended attributes
        flag: "--xattrs"
      preview: Preview
      repositories: Repositories
      schedule: Schedule
      schedule_placeholder: 0 2 * * *
      source_repository: Source repository
      source_repository_placeholder: Select a source repository
      submit_create: Create job
      submit_update: Update job
    index:
      empty: No jobs have been configured yet.
      new: New job
      subtitle: Manage synchronization tasks between repositories
      title: Jobs
    new:
      subtitle: Add a new synchronization job
      title: New job
    table:
      destination: Destination
      enabled: Enabled
      name: Name
      schedule: Schedule
      source: Source
    title: Jobs
    update:
      success: Job was successfully updated.
  monitoring:
    title: Background jobs
  page:
    info: Page %{current} of %{total}
    next: Next
    previous: Previous
  repositories:
    actions:
      delete: Delete repository
      delete_confirm: Are you sure you want to delete this repository? This action cannot be undone.
      delete_confirm_title: Delete repository
      edit: Edit repository
    create:
      success: Repository was successfully created.
    destroy:
      success: Repository was successfully deleted.
    edit:
      title: Edit repository
    form:
      description: Description
      description_placeholder: Optional description for this repository
      details: Details
      local: Local
      local_hint: This repository is stored on the machine running Rsync UI.
      location: Location
      name: Name
      name_placeholder: My repository
      path: Path
      path_placeholder: "/data/backups"
      read_only: Read-only
      remote: Remote
      server: Server
      server_placeholder: Select a server
      submit_create: Create repository
      submit_update: Update repository
      type: Type
    index:
      empty: No repositories have been configured yet.
      new: New repository
      subtitle: Manage local and remote directories
      title: Repositories
    new:
      subtitle: Add a new repository
      title: New repository
    table:
      name: Name
      path: Path
      read_only: Read-only
      server: Server
      type: Type
    title: Repositories
    types:
      local: Local
      remote: Remote
    update:
      success: Repository was successfully updated.
  servers:
    actions:
      delete: Delete server
      delete_confirm: Are you sure you want to delete this server? This action cannot be undone.
      delete_confirm_title: Delete server
      edit: Edit server
    create:
      success: Server was successfully created.
    destroy:
      success: Server was successfully deleted.
    edit:
      title: Edit server
    form:
      authentication: Authentication
      connection: Connection
      credentials_hint: Provide either a password or an SSH key (not both).
      description: Description
      description_placeholder: Optional description for this server
      details: Details
      host: Host
      host_placeholder: example.com
      name: Name
      name_placeholder: My server
      password: Password
      password_placeholder: Enter password
      password_placeholder_edit: Leave blank to keep unchanged
      port: Port
      port_placeholder: '22'
      ssh_key: SSH Key
      ssh_key_placeholder: "-----BEGIN OPENSSH PRIVATE KEY-----"
      ssh_key_placeholder_edit: Leave blank to keep unchanged
      submit_create: Create server
      submit_update: Update server
      username: Username
      username_placeholder: admin
    index:
      empty: No servers have been configured yet.
      new: New server
      subtitle: Manage remote SSH servers
      title: Servers
    new:
      subtitle: Add a new SSH server
      title: New server
    table:
      credentials: Credentials
      host: Host
      name: Name
      password: Password
      ssh_key: SSH Key
      username: Username
    title: Servers
    update:
      success: Server was successfully updated.
  settings:
    account:
      email: Email
      email_placeholder: email@example.com
      first_name: First name
      first_name_placeholder: John
      last_name: Last name
      last_name_placeholder: Doe
    delete_account:
      button: Delete my account
      confirm: Are you sure you want to delete your account? This will permanently delete all your data. This action cannot be undone.
      title: Delete Account
      warning: Deleting your account will permanently remove all your data!
    subtitle: Your personal preferences and account settings
    title: Settings
    update: Update
  shared:
    form_messages:
      error_title: Something went wrong
